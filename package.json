{
  "name": "dxcriteria",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build" : "npm run build:md && npm run build:book",
    "build:md" : "node ./bin/build.js",
    "build:book":"mdbook build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
  }
}
